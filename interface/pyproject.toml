# This project was generated with 0.3.22 using template: https://github.com/beeware/briefcase-template@v0.3.22
[tool.briefcase]
project_name = "Ophelia CI"
bundle = "com.planetae"
version = "0.6.0"
url = "https://planetae.com/ophelia-ci-interface"
license.file = "LICENSE"
author = "Edmilson Rodrigues"
author_email = "edmilson.monteiro.rodrigues@gmail.com"

requires = [
    "fastapi[all]>=0.115.11",
    "grpcio>=1.71.0",
    "grpcio-tools>=1.71.0",
    "jinja2>=3.1.6",
    "paramiko>=3.5.1",
    "protobuf>=5.29.3",
    "pydantic-settings>=2.8.1",
    "pydantic[email]>=2.10.6",
    "types-paramiko>=3.5.0.20240928",
    "types-protobuf>=5.29.1.20250315",
    "uvicorn[standard]>=0.34.0",
]

[tool.briefcase.app.ophelia-ci-interface]
formal_name = "Ophelia CI Interface"
description = "A web interface to interact with the Ophelia CI server"
long_description = """More details about the app should go here.
"""
sources = [
    "src/ophelia_ci_interface",
]
test_sources = [
    "tests",
]

requires = [
    # Add your cross-platform app requirements here
]
test_requires = [
    # Add your cross-platform test requirements here
]

[tool.briefcase.app.ophelia-ci-interface.macOS]
universal_build = true
requires = [
    # Add your macOS-specific app requirements here
]

[tool.briefcase.app.ophelia-ci-interface.linux]
requires = [
    # Add your Linux-specific app requirements here
]

[tool.briefcase.app.ophelia-ci-interface.linux.system.debian]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.ophelia-ci-interface.linux.system.rhel]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.ophelia-ci-interface.linux.system.suse]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.ophelia-ci-interface.linux.system.arch]
system_requires = [
    # Add any system packages needed at build the app here
]

system_runtime_requires = [
    # Add any system packages needed at runtime here
]

[tool.briefcase.app.ophelia-ci-interface.linux.flatpak]
flatpak_runtime = "org.freedesktop.Platform"
flatpak_runtime_version = "0.6.1"
flatpak_sdk = "org.freedesktop.Sdk"

[tool.briefcase.app.ophelia-ci-interface.windows]
requires = [
    # Add your Windows-specific app requirements here
]

# Mobile deployments
[tool.briefcase.app.ophelia-ci-interface.iOS]
requires = [
    # Add your iOS-specific app requirements here
]

[tool.briefcase.app.ophelia-ci-interface.android]
requires = [
    # Add your Android-specific app requirements here
]

# Web deployments
[tool.briefcase.app.ophelia-ci-interface.web]
requires = [
    # Add your web-specific app requirements here
]
